appTitle := "ahk_exe wezterm-gui.exe"
; appPath := "C:\\Program Files\\WezTerm\\wezterm-gui.exe"
appPath := "C:Users\\dufourz\\AppData\\Local\\Programs\\Tabby\\Tabby.exe"

^`::
{
    if (!WinExist(appTitle))
    {
        Run(appPath)
        return
    }

    if (WinActive(appTitle))
    {
        WinMinimize
        return
    }
    WinActivate(appTitle)
}
